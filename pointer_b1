#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a, b;

    printf("Nhap hai so: ");
    scanf("%d %d", &a, &b);

    int *p, *q;
    p = &a;
    q = &b;

    int sum = *p + *q;

    printf("\nTong cua %d va %d la %d\n", a, b, sum);

    return 0;
}
